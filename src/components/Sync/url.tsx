import * as React from 'react'
import { FormControl } from 'baseui/form-control'
import { Input } from 'baseui/input'

export const OktaUrl = () => {
  return (
    <FormControl label="Okta URL">
      <Input />
    </FormControl>
  )
}
